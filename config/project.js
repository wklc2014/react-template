/**
 * 项目相关配置
 */
module.exports = {
    // 是否将 jQuery 注入成全局变量
    useJqueryAsGlobal: true,
    // 将图片转化为 base64 的最小尺寸
    imageLimit: 25000,
    // 相关路径配置
    devServer: {
        host: 'localhost',
        port: 9000
    }
}
